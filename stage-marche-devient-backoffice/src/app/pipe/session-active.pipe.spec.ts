import { SessionActivePipe } from './session-active.pipe';

describe('SessionActivePipe', () => {
  it('create an instance', () => {
    const pipe = new SessionActivePipe();
    expect(pipe).toBeTruthy();
  });
});
